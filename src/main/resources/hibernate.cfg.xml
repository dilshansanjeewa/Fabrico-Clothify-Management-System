<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- JDBC Database Connection Settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/<!--YOUR DB NAME--></property>
        <property name="hibernate.connection.username"><!--YOUR DB USERNAME--></property>
        <property name="hibernate.connection.password"><!--YOUR DB PASSWORD--></property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- JDBC connection pool settings -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- Show SQL statements in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>
        <property name="hibernate.highlight_sql">true</property>

        <!-- Enable Hibernate to automatically create/drop tables -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping Entity Classes -->
        <mapping class="model.entity.EmployeeEntity"/>
        <mapping class="model.entity.UserEntity"/>
        <mapping class="model.entity.AdminEntity"/>
        <mapping class="model.entity.SupplierEntity"/>
        <mapping class="model.entity.BrandEntity"/>
        <mapping class="model.entity.ItemEntity"/>
        <mapping class="model.entity.OrderEntity"/>
        <mapping class="model.entity.OrderDetailEntity"/>

    </session-factory>
</hibernate-configuration>